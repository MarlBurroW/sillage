{{- if .Values.config }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sillage.fullname" . }}-config
  labels:
    {{- include "sillage.labels" . | nindent 4 }}
data:
  config.toml: |
    {{- .Values.config | nindent 4 }}
{{- end }}
